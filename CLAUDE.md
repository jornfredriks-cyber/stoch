# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## Project Overview

Standalone toolkit implementing the "Weekly Stochastic Sweet Spot" trend-following strategy (`Weekly Stochastic Sweet Spot Strategy.md`, sourced from a YouTube video — treat it as reference material, don't edit it with derived analysis). Originally built inside `Darvas Phase3 Scan/`, relocated to its own project with its own venv/config/tests on 2026-07-14 so it has no dependency on that project.

Three-stage pipeline, same shape as `Darvas Phase3 Scan/`:

| Stage | Script | Purpose | Output |
|---|---|---|---|
| 1 | `screener.py` | Pre-filter NASDAQ+NYSE universe (price/volume/EMA/ADR/RSI/ATH) | `OUTPUT/Screener/Stoch Raw Screener_YYYY-MM-DD.csv` |
| 2 | `stoch_scan.py` | Live scan: flag tickers currently inside the 32–80 sweet-spot band, %K>%D, rising | `OUTPUT/SweetSpot/Stoch_SweetSpot_YYYY-MM-DD.txt` |
| 3 | `backtest.py` | Backtest the strategy's long entry/exit rules against full historical data | `OUTPUT/Stoch_Backtest_Trades_YYYY-MM-DD.csv` |
| 3b | `backtest_signal.py` | Separate backtest of the "Stochastic Weekly Signal" Pine indicator's exact crossover/crossunder circle events (fixed $1000/trade, no SL/TP). Universe: newest file in `INPUT/` if one exists (any name/format), else the Darvas Raw screener CSV — **different rule set from `backtest.py`**, see below | `OUTPUT/StochSignal_Backtest_Trades_YYYY-MM-DD.csv` |
| 3c | `backtest_signal_sweep.py` | Parameter sweep: grid-searches `stoch_length`/`k_smooth`/`d_smooth` (entry/exit thresholds fixed at 32/80) on top of `backtest_signal.py`'s exact rule set, ranks combos by total $ P&L | `OUTPUT/StochSignal_Sweep_YYYY-MM-DD.csv` |
| 4 | `stoch_signal_scan.py` | Live buy-candidate scan: same exact crossover rule as `backtest_signal.py` (fired on the **latest** weekly bar) **plus** an `is_not_stretched()` gate (current daily close within 0-5% above daily EMA50 — not chasing an extended move), against the Darvas Raw screener universe | `OUTPUT/StochSignal_Candidates_YYYY-MM-DD.csv` |
| 3d | `backtest_signal_sltp_sweep.py` | Adds an SL/TP overlay + optional weekly EMA50>EMA200 long-term trend filter on top of `backtest_signal.py`'s exact crossover rule; sweeps SL 4-10% × TP 1.5-3.5R × filter on/off (70 combos), ranks by total $ P&L, includes a per-ticker win/loss breakdown + trade-level CSVs for the top 3 combos | `OUTPUT/StochSignal_SLTP_Sweep_YYYY-MM-DD.md` |
| 4b | `stoch_fresh_scan.py` | One-operation live buy-candidate scan (`screener.py` → `stoch_fresh_scan.py`, same shape as stage 1→2): flags tickers whose weekly %K crossed strictly above `entry_level` (default 32) within the last `lookback_weeks` (default 2) **fully completed** weekly bars — the current, still-forming week is always excluded — with %K>%D on that bar. Uses Stoch's own screener universe, not the Darvas one; no EMA50 "not stretched" gate | `OUTPUT/Screener/Stoch Raw Screener_YYYY-MM-DD.csv`, `OUTPUT/StochFresh_Candidates_YYYY-MM-DD.csv` |
| 1c/5 | `stoch_basic_screener.py` → `stoch_basic_scan.py` | Watchlist scanner for **declined** names, not breakouts. A deliberately loose full-market pre-filter (price/volume/market-cap/ADR only — no `ema_fast`/`ema_slow` uptrend requirement, no `ath_max_dist_pct` proximity-to-high requirement, unlike every other screener here), then flags tickers whose latest Weekly Stochastic bar has %K above %D but still below `k_ceiling` (default 32) — the window *before* a sweet-spot/signal buy trigger, not the trigger itself. Pure snapshot check, no "rising" requirement | `OUTPUT/BasicScreener/Stoch Basic Screener_YYYY-MM-DD.csv`, `OUTPUT/BasicScan/Stoch_BasicScan_YYYY-MM-DD.txt` |
| 6 | `backtest_basic_trail.py` | Backtests the Basic Scanner's premise: same entry crossover as `backtest_signal.py` (%K crosses above `entry_level`, %K>%D), but the exit is **only** a `trail_pct`% trailing stop off the highest weekly close since entry, checked against daily intraweek lows — no signal exit, no take-profit, no time limit. Ticker source: `INPUT/`, else the newest `OUTPUT/BasicScan/` output. Pure % returns, no dollar sizing | `OUTPUT/BasicTrail_Backtest_Trades_YYYY-MM-DD.csv` |

## Running

```bash
cd "Stoch"

# Live scan pipeline (screener → sweet-spot scan)
venv/bin/python3 screener.py && venv/bin/python3 stoch_scan.py
# or double-click "RUN Stoch Sweet Scan.command"

# Backtest (reads ticker list from INPUT/, or falls back to the screener CSV)
venv/bin/python3 backtest.py
# or double-click "RUN Stoch Backtest.command"

# Signal backtest (reads the newest file in INPUT/ if present, else the Darvas
# Raw screener CSV -- fixed $1000/trade)
venv/bin/python3 backtest_signal.py
# or double-click "RUN Stoch Signal Backtest.command"

# Signal parameter sweep (finds best stoch_length/k_smooth/d_smooth by total $ P&L)
venv/bin/python3 backtest_signal_sweep.py
# or double-click "RUN Stoch Signal Sweep.command"

# Live buy-candidate scan (always reads the Darvas Raw screener CSV; fresh
# crossover + not-stretched-above-EMA50 gate)
venv/bin/python3 stoch_signal_scan.py
# or double-click "RUN Stoch Signal Scan.command"

# SL/TP + long-term trend filter sweep on top of backtest_signal.py's rule
# (reads the same INPUT/-or-Darvas universe as backtest_signal.py)
venv/bin/python3 backtest_signal_sltp_sweep.py
# or double-click "RUN Stoch Signal SLTP Sweep.command"

# Live fresh-buy-candidate scan, one operation (Stoch's own screener → fresh
# crossover within the last 2 completed weeks, no EMA50 gate)
venv/bin/python3 screener.py && venv/bin/python3 stoch_fresh_scan.py
# or double-click "RUN Stoch Fresh Scan.command"

# Basic Scanner: watchlist for declined names that might be turning (loose
# screener, no trend/ATH-proximity filter -> %K>%D and %K<32 on the latest
# weekly bar)
venv/bin/python3 stoch_basic_screener.py && venv/bin/python3 stoch_basic_scan.py
# or double-click "RUN Stoch Basic Scan.command"

# Basic Trail Backtest: same entry crossover as the Signal backtest, but
# the ONLY exit is a 20% trailing stop off the highest weekly close since
# entry (reads INPUT/, or falls back to the latest Basic Scan output)
venv/bin/python3 backtest_basic_trail.py
# or double-click "RUN Stoch Basic Trail Backtest.command"

# Tests
venv/bin/python3 -m pytest tests/ -v
```

## Configuration

All tunable parameters live in `stoch_config.ini` — don't hardcode values in the Python files.

- `[screener]` — pre-filter thresholds (price, market cap, volume, ADR, EMA trend, RSI, ATH proximity). Mirrors Darvas Phase3 Scan's screener defaults.
- `[stoch_scanner]` — Stochastic settings (`stoch_length`/`stoch_k_smooth`/`stoch_d_smooth`, currently 19/4/4 — the strategy doc's original settings. Briefly swapped to the `backtest_signal_sweep.py`-optimized 3/7/7 combo on 2026-07-23, reverted back to 19/4/4 on 2026-07-30 — this is the current, intentional setting) and the sweet-spot band (`stoch_k_min`=32.0, `stoch_k_max`=80.0). **`backtest.py` and `backtest_signal.py` both read these same constants from `stoch_scan.py` — there is no separate `[backtest]` section.** The 32/80 band doubles as both backtests' entry/exit levels — they share the same Stochastic settings, only the crossing rule differs (see below).
- `[backtest_signal]` — `trade_value_usd` (default 1000.0), the fixed dollar notional `backtest_signal.py` assumes per trade (no share-count rounding: `dollar_pnl = trade_value_usd * (exit_price / entry_price - 1)`).
- `[stoch_signal_scan]` — `ema_length`/`ema_band_min_pct`/`ema_band_max_pct` (50 / 0.0 / 5.0) for `stoch_signal_scan.py`'s `is_not_stretched()` gate: current daily close must sit within this % band above its daily EMA(`ema_length`). Retired `ema50_screener.py`'s standalone full-market pre-filter (2026-07-26) folded into this one check — the Darvas Raw screener already handles price/volume/ADR/market-cap/trend/RSI/ATH-proximity quality upstream, so only the genuinely new idea (EMA-proximity) needed to move over.
- `[stoch_fresh_scan]` — `lookback_weeks`/`entry_level` (2 / 32.0) for `stoch_fresh_scan.py`'s crossover check: %K must cross strictly above `entry_level`, with %K>%D, within the last `lookback_weeks` fully completed weekly bars (current, still-forming week always excluded). `entry_level` is independent of `[stoch_scanner]`'s `stoch_k_min` (same value today, but a distinct concept — an entry-crossover threshold vs. a persistent-band floor). No EMA/not-stretched keys here — this scan has no such gate, unlike `[stoch_signal_scan]`.
- `[backtest_signal_sltp_sweep]` — `ema_long_fast`/`ema_long_slow` (50/200), the fixed periods for `backtest_signal_sltp_sweep.py`'s optional `"long_trend"` entry filter (weekly EMA50>EMA200 — a regime-level check, ~1yr vs ~4yr; replaced an earlier short EMA10>EMA20 crossover, see Architecture below). The SL/TP grids themselves (`SL_GRID`/`TP_GRID`/`FILTER_GRID`) are **module-level constants at the top of the script**, not in this config section — same convention as `backtest_signal_sweep.py`'s stoch-param grids (a sweep's input space, not a single tunable).
- `[stoch_basic_screener]` — `min_price`/`min_market_cap`/`min_avg_vol_30d`/`min_adr_pct` (10.0 / 300M / 1M / 2.0), a deliberately loose subset of `[screener]`'s knobs with **no `ema_fast`/`ema_slow` and no `ath_max_dist_pct`** — those two filters actively exclude declined stocks, which is the whole point of this scanner. Start minimal, tighten later once real output is seen (explicit user decision, 2026-08-27).
- `[stoch_basic_scan]` — `k_ceiling` (32.0) for `stoch_basic_scan.py`'s snapshot check: latest weekly %K must be above %D and below `k_ceiling`. Kept independent of `[stoch_scanner]`'s `stoch_k_min` (same value today, distinct concept — same reasoning as `[stoch_fresh_scan]`'s `entry_level`). Stochastic length/smoothing are reused from `[stoch_scanner]`, not duplicated here.
- `[backtest_basic_trail]` — `entry_level` (32.0, independent key, same reasoning as every other backtest/scan section) and `trail_pct` (20.0) for `backtest_basic_trail.py`. No `trade_value_usd` — this backtest is pure % returns by explicit user decision (2026-08-27), not dollar-notional.

## Architecture

**`scan_utils.py`** — shared utility layer, no strategy logic:
- `fetch_ohlc_bulk(yf_symbols, period=HISTORY_PERIOD, auto_adjust=True)` — chunked yfinance download with retry. `period` defaults to `"1y"` (what `screener.py`/`stoch_scan.py` need) but `backtest.py`/`backtest_signal.py` override it to `"max"` for full history. `auto_adjust` defaults to `True` (splits + dividends fully adjusted, matching `screener.py`/`stoch_scan.py`/`backtest.py`'s long-standing behavior) — `backtest_signal.py` passes `auto_adjust=False` instead (split-adjusted only). See the dividend-adjustment note under `backtest_signal.py` below for why.
- `fetch_ohlc_single`, `find_latest_csv`, `_Tee` (stdout+logfile tee)
- `find_ticker_list(input_folder, fallback_folder)` — resolves the ticker universe. Looks in `input_folder` (i.e. `INPUT/`) for a file matching `*Watchlist*` **or** TradingView's actual default export naming `[Tt]radingview*` (confirmed in practice: `Tradingview <ListName>_<date>_<id>.txt` — it never contains the word "Watchlist"). Falls back to the latest `*Screener*.csv` in `fallback_folder` if neither is found. Only `backtest.py` uses this today — `screener.py`/`stoch_scan.py` still read the screener CSV directly.
- `_parse_watchlist_line(line)` — parses `EXCHANGE:TICKER,EXCHANGE:TICKER` (TradingView watchlist export format), converting `OSLO:` to a `.OL` yfinance suffix. Same convention as `Dashboard/data_loader.py`'s `parse_ticker_line`.

**`stoch_scan.py`** — live scanner + the shared Stochastic math:
- `compute_kd(df, stoch_length, k_smooth, d_smooth)` — resamples daily OHLC to Friday-anchored weekly bars and computes %K/%D. Returns a DataFrame (`close`, `k`, `d`) indexed by weekly bar. The final (possibly still-forming) week is included as-is, matching how TradingView plots a live weekly Stochastic — deliberate, not an oversight. `backtest.py` reuses this directly so the backtest and live scan always agree on the math.
- `is_stoch_sweet_spot(df, ...)` — calls `compute_kd`, then checks: latest %K inside `[k_min, k_max]`, %K > %D, %K rising vs. `k_rising_lookback` weeks back. Returns `(True, k, d)` or `(False, None, None)`.

**`backtest.py`** — trade simulation:
- `simulate_trades(weekly, entry_level=32.0, exit_level=80.0)` — pure function, no I/O. Takes a `compute_kd`-shaped DataFrame and returns a list of trade dicts. State machine, confirmed rules (see below). Fully unit-tested in isolation from OHLC/resampling concerns.
- `run_backtest(folder)` / `main()` — orchestration: `find_ticker_list` → `fetch_ohlc_bulk(period="max")` → `compute_kd` → `simulate_trades` per ticker → CSV + log + console summary. Wraps the per-ticker loop in try/except (one bad ticker doesn't abort the run) and de-dupes the ticker list before processing (a duplicate ticker would otherwise double-count its trades into the win-rate stat).

### Confirmed backtest rules (long-only)

- **Entry**: first week %K crosses above 32 (prior week <32, current week ≥32). Pure %K level-cross — %D not checked. Fill at that week's close.
- **Exit**: first week *after entry* where %K < %D **and** %K < 80 are simultaneously true. This holds through %K≥80 even if %K has already crossed below %D ("ride it"), and holds below 80 as long as %K stays above %D. Fill at that week's close.
- No stop loss. No portfolio/capital simulation — each trade is an isolated % return. A ticker can produce multiple sequential trades over its history.
- Still-open positions when data ends are recorded as `open`, marked-to-market at the last close, excluded from win/loss stats.
- **Not yet implemented**: short side, parameter sweep across Stochastic settings, a trailing-stop/breakeven variant of the exit rule (see Open Threads below).

### `backtest_signal.py` — different rule set, do not confuse with `backtest.py`

Backtests the "Stochastic Weekly Signal" Pine indicator's literal `buySignal`/`sellSignal` circle-plot conditions, not the strategy doc's rules above.

- **Ticker universe (`load_backtest_signal_tickers()`, added 2026-07-25)**: if `INPUT/` contains any non-hidden file, uses the newest one by mtime — no filename or content-format requirement (parsed the same way as a watchlist: one ticker per line, or comma-separated `EXCHANGE:TICKER`, via `_parse_watchlist_line`). This is deliberately looser than `scan_utils.find_ticker_list()`'s `*Watchlist*`/`[Tt]radingview*`-only glob (used by `backtest.py`) — drop in e.g. a hand-picked ticker list `.txt` as-is, no renaming needed. Falls back to the latest `DARVAS Raw Screener_*.csv` in `../Darvas Phase3 Scan/` (via `find_latest_csv`) if `INPUT/` has no files, same as this script's behavior before `INPUT/` support existed.

- **Entry (green)**: %K crosses *strictly* above `stoch_k_min` (prior week %K < 32, current week %K > 32) **and** %K > %D on the current week.
- **Exit (red)**: %K crosses *strictly* below `stoch_k_max` (prior week %K > 80, current week %K < 80) **and** %K < %D on the current week.
- No stop loss, no take profit. Fixed `trade_value_usd` (1000.0) notional per trade — `dollar_pnl` reported alongside `return_pct`.
- **Quirk, not a bug**: because exit requires an actual crossunder-of-80 event, a trade whose %K never climbs back above 80 after entry will never get an exit signal under this literal rule and stays `open` until the ticker's data ends. Expect a non-trivial open-trade count in the summary — that's this rule doing what the Pine source says, not a broken backtest.
- `simulate_trades_signal()` is intentionally a separate function from `backtest.py`'s `simulate_trades()` — same state-machine shape, different (stricter, event-based) crossing conditions. Don't merge them; they answer different questions.
- **Raw chart circles will outnumber the trade log — expected, not a discrepancy.** The Pine indicator's `plotshape()` draws a circle every time the raw `buySignal`/`sellSignal` condition is true, with no memory of position state. `simulate_trades_signal()` deliberately only counts the *first* green after being flat and the *first* red after being long (ignoring buy signals that fire while already holding, and sell signals that fire while already flat) — matching the single-position, $1000-no-pyramiding framing the backtest was built for. Verified 2026-07-21 on AAPL/weekly (`periodK=3,smoothK=7,periodD=7`): raw signals included extra BUYs on 2022-06-24/2022-10-28 (already holding since 2021-04-09, correctly ignored) and extra SELLs on 2023-12-29/2025-10-10 (already flat, correctly ignored) — only 2023-07-14, 2024-04-26/07-19, and 2026-04-10/06-12 made it into the actual trade list. When eyeballing the indicator against a CSV trade log, expect this gap and don't treat it as a bug.
- **Dividend-unadjusted prices (`auto_adjust=False`)**: unlike every other script in this project, `backtest_signal.py` fetches with `auto_adjust=False`. Confirmed by manual verification (2026-07-21) against the live TradingView indicator on ABBV: `auto_adjust=True` (the project default) fully adjusts historical OHLC for dividends as well as splits, and ABBV alone has paid 55 dividends since 2013 with zero splits — the adjustment ratio drifts over time (e.g. 0.64 in Nov 2015 vs 0.67 in Nov 2016, since `auto_adjust=True` rescales the whole history relative to *today's* cumulative dividends every time it's fetched). This measurably shifted the Stochastic's rolling-window %K/%D, causing wrong exit weeks and fully missed trade cycles versus what the chart actually plots (TradingView shows raw/unadjusted prices by default). Switching to `auto_adjust=False` (split-adjusted only — verified against AAPL's Aug 2020 4:1 split showing no price discontinuity) resolved 4 of 5 discrepancies found in that ABBV spot-check exactly, and reduced the 5th from ~17 days off to ~10 days off (residual gap likely Yahoo-vs-TradingView data-vendor differences in daily H/L, not adjustment). This fix is scoped to `backtest_signal.py` only — `backtest.py`/`stoch_scan.py`/`screener.py` still use the default `auto_adjust=True`, so their previously-documented results (see Open Threads below) are unaffected.

### `stoch_signal_scan.py` — live buy-candidate scan, same rule as `backtest_signal.py`

Answers "which Darvas Raw screener tickers have a fresh buy signal *right now*, that isn't already an extended move," instead of backtesting full history. Reuses `backtest_signal.load_darvas_raw_tickers()` and `stoch_scan.compute_kd()` directly — no duplicated math.

- `is_fresh_stoch_signal_buy(df, ...)` — checks only the **last two valid weekly bars**: prior bar's %K < 32, current bar's %K > 32 (strict), %K > %D on the current bar. Same entry condition as `simulate_trades_signal()`'s buy leg, but evaluated once against the latest bar instead of iterating the full trade history — deliberately cheaper than running the whole state machine just to answer a "did it fire this week" question. Returns `(True, k, d, entry_date, entry_price)` or `(False, None, None, None, None)`.
- `is_not_stretched(df, ...)` (added 2026-07-26) — the surviving idea from the retired `ema50_screener.py` supplement (see Open Threads below): requires the current daily close to sit within `ema_band_min_pct`-`ema_band_max_pct`% (default 0-5%) above its daily EMA(`ema_length`, default 50). Drops any trailing NaN close row first (the yfinance `auto_adjust=True` gotcha from 2026-07-25 — see `feedback-yfinance-trailing-nan` memory). A candidate only makes the final list if **both** `is_fresh_stoch_signal_buy()` and `is_not_stretched()` pass — the CSV output includes `ema50_dist_pct` so you can see how close each survivor actually is.
- Buy-only by design: sell signals only matter for positions actually held, and this scanner has no notion of your holdings.
- Same `auto_adjust=False` fetch as `backtest_signal.py`, same rationale (unadjusted prices match what the live Pine indicator actually plots) — this is a separate fetch from the trailing-NaN issue above, which is specific to `auto_adjust=True` fetches elsewhere in the project.
- Ticker universe freshness: like every other script here, it just takes whichever `DARVAS Raw Screener_*.csv` is newest by file mtime — re-run `../Darvas Phase3 Scan/darvas_screener.py` yourself first if you want a same-day universe.
- Output: `OUTPUT/StochSignal_Candidates_YYYY-MM-DD.csv` (`ticker`, `entry_date`, `entry_k`, `entry_d`, `price`, `ema50_dist_pct`) — one row per fresh, not-stretched candidate, no historical trades.

### `stoch_fresh_scan.py` — one-operation fresh-crossover scan, Stoch's own universe

Answers "which of Stoch's own screener candidates had a fresh weekly Stochastic buy crossover in the last week or two — not just this exact instant?" A sibling to both `stoch_scan.py` (same engine: `compute_kd()`, `[stoch_scanner]` settings, Stoch's own screener output) and `stoch_signal_scan.py` (same crossover-event shape), but combining traits neither one has alone.

- `is_fresh_stoch_buy_within_lookback(df, ..., entry_level, lookback_weeks)` — checks the last `lookback_weeks` **fully completed** weekly bars (default 2) for a strict crossover: prior week's %K < `entry_level`, that week's %K > `entry_level`, %K > %D on that week. **The current, possibly still-forming week is always dropped before any check runs** (`kd.iloc[:-1]`, done before `dropna()` so a NaN live bar can't be mistaken for a real completed week) — this is the key behavioral difference from `stoch_signal_scan.is_fresh_stoch_signal_buy()`, which deliberately treats the live bar as eligible. If more than one week in the window qualifies, the most recent one wins. Returns `(True, k, d, entry_date, entry_price, weeks_ago)` or `(False, None, None, None, None, None)`.
- **Universe**: `find_latest_csv(folder)` on the Stoch project root — Stoch's own `screener.py` output, same as `stoch_scan.py` — **not** the Darvas Phase3 Scan project's screener that `stoch_signal_scan.py` depends on. This is what makes `RUN Stoch Fresh Scan.command` a true single self-contained operation (`screener.py && stoch_fresh_scan.py`), unlike `stoch_signal_scan.py` which silently relies on whichever Darvas screener CSV happens to already exist.
- **No EMA50 "not stretched" gate** — a deliberate omission versus `stoch_signal_scan.py`. The only qualifying condition is the fresh crossover itself.
- **Dividend-unadjusted prices (`auto_adjust=False`)**: same fix as `backtest_signal.py`/`stoch_signal_scan.py` (see the dividend-adjustment note under `backtest_signal.py` above), applied here for an independent reason — this scan's whole purpose is precise crossover-*timing* detection, which `auto_adjust=True`'s retroactive dividend rescaling directly corrupts. Confirmed on EQNR (2026-07-30): with `auto_adjust=True`, the real crossover (a clean one-week jump from %K=31.5 to 44.1 per the user's own TradingView export, unadjusted) got smeared across two of our weekly bars — %K crept above 32 a week early but stayed below %D that week, then by the next week %K had already been above 32 too long to register as "fresh" — so the signal was missed by the 2-week lookback entirely, in both directions. Recomputing with `auto_adjust=False` matched the TradingView export almost exactly (%K 44.08 vs. 44.09, %D 34.50 vs. 34.51) and correctly classified it as `weeks_ago=1`. First run under this fix (2026-07-30, 488-ticker screener universe post the user's `ath_max_dist_pct` 5%→10% widening) found 7 candidates: CHRD, SFD, BDX, CF, EPRT, EQNR, WEX — up from 1 (WEX only) under the original `auto_adjust=True` run.
- Buy-only by design, same rationale as `stoch_signal_scan.py`.
- Output: `OUTPUT/StochFresh_Candidates_YYYY-MM-DD.csv` (`ticker`, `weeks_ago`, `entry_date`, `entry_k`, `entry_d`, `price`) — `weeks_ago` placed early since distinguishing a 1-week-old from a 2-week-old signal is the whole point of this scan.

### `backtest_signal_sltp_sweep.py` — SL/TP overlay + trend filter sweep on `backtest_signal.py`'s rule

Answers "does adding a stop-loss/take-profit and a long-term trend entry filter improve on the pure signal-only exit?" Reuses `backtest_signal.load_backtest_signal_tickers()` (same `INPUT/`-or-Darvas universe resolution) and `stoch_scan.compute_kd()`/`resample_weekly()` directly.

- `build_weekly_frame(daily)` — weekly `close`/`k`/`d`/`ema_long_fast`/`ema_long_slow`, computed **once per ticker** and shared across all 70 grid combos (none of the swept parameters change these values, only how `simulate_trades_signal_sltp()` interprets them) — same resample-once-reuse-many optimization as `backtest_signal_sweep.py`.
- `simulate_trades_signal_sltp(daily, weekly, sl_pct, tp_r, filter_type, ...)` — same exact entry crossover as `simulate_trades_signal()`, optionally gated (`filter_type="long_trend"`) by weekly `ema_long_fast > ema_long_slow` (default 50/200, ~1yr vs ~4yr — a regime-level check, not a short-term crossover) on the entry bar. `stop_price = entry_price*(1-sl_pct/100)`; `target_price = entry_price + tp_r*(entry_price-stop_price)` (TP is an **R-multiple of the SL risk**, not a flat %). Once in a position, checks **daily intraweek highs/lows** (not just the weekly close) for the first day whose low breaches `stop_price` or high breaches `target_price` — exits there, at the stop/target price itself. If no daily touch that week, falls through to the original signal exit (`simulate_trades_signal()`'s crossunder-of-80 condition) at that week's close. **Priority: SL/TP touch (checked daily) wins over the signal exit (checked weekly)** — whichever fires first in date order. If a single day's range touches both SL and TP, SL is assumed to hit first (conservative worst-case tie-break; raw OHLC can't reveal true intraday sequencing). Reports `holding_days` instead of `holding_weeks` since exits can now land mid-week.
- Grid: `SL_GRID=[4,5,6,7,8,9,10]`% × `TP_GRID=[1.5,2.0,2.5,3.0,3.5]`R × `FILTER_GRID=["none","long_trend"]` = 70 combinations, all module-level constants at the top of the file (widen/narrow as needed, same convention as `backtest_signal_sweep.py`'s stoch-param grids).
- **2026-07-26 pivot**: the filter was originally a short weekly EMA10>EMA20 crossover (boolean), tested first and found inconclusive on small sample sizes. Replaced with the long-term `EMA50>EMA200` regime filter above after per-ticker analysis showed NOG/EQT's losses cluster inside multi-month/year downtrends, not short-term noise — a regime-level filter targets that directly. `use_ema_filter: bool` became `filter_type: str` (`"none"`/`"long_trend"`) to make room for future filter types without another boolean-to-something rewrite.
- Output: a single markdown file, `OUTPUT/StochSignal_SLTP_Sweep_YYYY-MM-DD.md` — a sweep-results table (all 70 combos, sorted best-first by total $ P&L, with SL/TP/signal exit-reason counts per row) plus a per-ticker wins/losses/win% table for each of the top 3 ranked combos. Trade-level CSVs (`OUTPUT/StochSignal_SLTP_Trades_Rank1/2/3_<date>.csv` — full entry/exit dates, prices, exit reason) are also saved for the top 3, for drilling into individual tickers' trade history.
- Ships with `RUN Stoch Signal SLTP Sweep.command`. 9 tests in `tests/test_backtest_signal_sltp_sweep.py` covering SL-hit, TP-hit, same-day-tie-break, signal-exit-when-no-touch, long-trend-filter gating both directions, R-multiple math, and open-trade mark-to-market.
- **Findings (2026-07-26)**: SL dominates the exit mix (~68% of closed trades in the best combo) — breakeven win rate depends only on the TP multiple (`1/(1+TP_r)`, e.g. 22.2% at TP=3.5R), and a ticker's actual TP-hit rate relative to that threshold determines whether it's a net winner. NOG/EQT sit below breakeven at high TP (structural, not a tuning problem); NVDA sits well above it. The `long_trend` filter **cuts NOG/EQT's loss counts substantially** (e.g. at SL=9%/TP=3.5R: NOG 17→5 losses, flips to slightly positive P&L; EQT 30→19 losses) but also **cuts NVDA's best trades**, shrinking its total $ contribution enough that the filtered aggregate underperforms the unfiltered one overall — the filter helps the weak/choppy names specifically but isn't a free win across a mixed universe.

### `backtest_signal_sweep.py` — parameter sweep on top of `backtest_signal.py`

Grid-searches `stoch_length` × `k_smooth` × `d_smooth` (`LENGTH_GRID`/`K_SMOOTH_GRID`/`D_SMOOTH_GRID` constants at the top of the file — widen/narrow as needed) to find the combo that maximizes **total $ P&L across closed trades** (not win rate) for the same fixed 32/80 entry/exit thresholds and `simulate_trades_signal()` rule set as `backtest_signal.py`. Per user decision (2026-07-21): entry/exit thresholds are held fixed, not swept — only the three Stochastic settings.

- Fetches each ticker's OHLC **once** (`auto_adjust=False`, same as `backtest_signal.py`) and resamples to weekly **once** (`stoch_scan.resample_weekly()`, extracted from `compute_kd()` for this reuse) — the parameter grid then loops purely over cached in-memory weekly frames via `compute_kd_fast()` (same math as `compute_kd()`, just skipping the redundant resample per combo). This is what keeps a few-hundred-combo sweep to minutes instead of hours.
- Output: `OUTPUT/StochSignal_Sweep_YYYY-MM-DD.csv` — one row per combo (`stoch_length`, `k_smooth`, `d_smooth`, `total_trades`, `closed`, `open`, `win_rate_pct`, `avg_win_pct`, `avg_loss_pct`, `total_pnl`), sorted best-first. Console/log also prints the top 10 and the single best combo's `periodK`/`smoothK`/`periodD` values in Pine-input terms, ready to type into the indicator on the actual chart for visual confirmation.
- `tests/test_backtest_signal_sweep.py` verifies `compute_kd_fast()` produces identical output to the canonical `stoch_scan.compute_kd()` for several parameter combos on synthetic data — regression insurance that the resample-once optimization never silently diverges from the tested calculation.
- **Watch for grid-floor artifacts when picking a "best" combo.** The first sweep (`range(8,32,2)`) had every top-10 combo land on `length=8`, the grid's own floor — total $ P&L was still rising as length fell, so 8 wasn't a real optimum. Extending to `length=3` (see `LENGTH_GRID` history in the constant's comment) reproduced the same pattern at the new floor. Since length=3 is at (or one step from) the mathematical minimum for a Stochastic window, this was accepted as final rather than chased down to length=2. **Also distrust raw ranking-by-total-$-P&L for very-high-trade-count combos** — `k_smooth=2` variants at length=3 racked up 12,000–24,000 closed trades (vs. ~2,000 for the eventual winner) purely from firing more often; total $ P&L doesn't penalize trade volume, so those are noise-trading artifacts of the ranking metric, not genuinely better settings, even when they outrank cleaner combos.

### `stoch_basic_screener.py` / `stoch_basic_scan.py` — watchlist scanner for declined names

Answers a different question from every other scanner in this project: "which names have **declined** and might be starting to turn?" instead of "which names are already breaking out?" Built 2026-08-27 after the user observed that the Sweet Spot/Signal/Fresh scanners' candidates were consistently stretched (near highs) — because their shared universe (the Darvas-style `[screener]` filters: `ema_fast>ema_slow` uptrend, `ath_max_dist_pct` proximity-to-high) actively selects for that.

- **`stoch_basic_screener.py`** — a full NASDAQ+NYSE pass, structurally identical to `screener.py` (same `fetch_universe()`/`fetch_ohlc_bulk()` pipeline, now shared via `scan_utils.py`) but with a deliberately loose `_passes()`: only `min_price`, `min_avg_vol_30d`, `min_adr_pct`, and a `min_market_cap` floor. No EMA-trend check, no RSI band, no ATH-proximity check — all three would exclude the exact names this scanner exists to find. Output: `OUTPUT/BasicScreener/Stoch Basic Screener_YYYY-MM-DD.csv`.
- **`stoch_basic_scan.py`** — `is_stoch_basic_candidate()` checks only the **latest** weekly bar: %K above %D and %K below `k_ceiling` (default 32.0). Deliberately simpler than `is_stoch_sweet_spot()` — no "rising vs. N weeks back" requirement — per explicit user decision (2026-08-27): a pure snapshot of "still below the sweet-spot floor, but curling above %D," meant to build a watchlist to track as the turn develops, not to trigger an entry. Reuses `compute_kd()` and `[stoch_scanner]`'s 19/4/4 settings directly — no new stochastic parameters. Output: `OUTPUT/BasicScan/Stoch_BasicScan_YYYY-MM-DD.txt` — a plain-text ticker list, one per line (same format as the Sweet Spot scan's output), per explicit user request (2026-08-27): direct import into a TradingView watchlist, no other technical data needed in the file itself (K/D/price are still printed to the console/log during the run for your own review, just not saved to the output file).
- **Shared-code cleanup done alongside this build**: `screener.py`'s `_fetch_universe()` and `_adr()` moved into `scan_utils.py` (as `fetch_universe()`/`adr()`) so `stoch_basic_screener.py` didn't have to copy-paste them. No behavior change to `screener.py`.
- Ships with `RUN Stoch Basic Scan.command`. 5 tests in `tests/test_stoch_basic_scan.py`, full suite 70/70 passing.
- **Explicit design decisions, all per user (2026-08-27)**: bare-minimum filters only, tightened later once real output is seen (not now); a $300M market-cap floor (not $100M, not zero); no "%K rising" requirement (unlike the Sweet Spot scan) — a pure current-bar snapshot, exactly as described, nothing more.

### `backtest_basic_trail.py` — trailing-stop backtest on the Basic Scanner's own candidates

Answers "what if I entered on every Signal buy crossover among the Basic Scanner's declined-then-turning candidates, and just trailed a stop instead of using a signal exit?" Built 2026-08-27 via brainstorming (bounded path), design negotiated across several rounds — see that date's Open Threads entry below for the locked decisions.

- `simulate_trades_basic_trail(daily, weekly, entry_level, trail_pct)` — pure state machine, same shape as `backtest.py`'s `simulate_trades()` and `backtest_signal.py`'s `simulate_trades_signal()`. Entry: `simulate_trades_signal()`'s exact buy leg, unchanged. Exit: **only** `trail_pct`% below the highest weekly close since entry (`stop = max(stop, highest_close * (1 - trail_pct/100))`, monotonically increasing) — no take-profit, no signal exit, no time limit; a trade stays open indefinitely if the stock just keeps climbing.
  - **Revised 2026-08-27** (same day, second pass): originally checked only at each weekly close. A live run against the 94-ticker Basic Scan watchlist found a trade (VERA) that lost **-72.46%** on a nominal -20% stop — a single week's close can land arbitrarily far past the stop under a close-only check, and separately, a real intraweek touch could be *missed entirely* if price recovered by that Friday's close. Now checks against each **daily low** within the week instead (`_find_stop_touch()`, same technique `backtest_signal_sltp_sweep.py` already uses for its SL/TP), filling at the stop price itself the moment a day's low touches it — matches how a real resting stop order would actually behave, at the cost of needing the raw daily OHLC frame (`daily`) alongside `weekly` now.
  - **Sequencing detail worth remembering**: each week's daily lows are checked against the stop as it stood at the *end of the prior week* — never a stop that week's own (not-yet-known-until-Friday) close would newly justify. Only after a week survives with no touch does its close get folded into the ratchet for the following week. Getting this backwards would be look-ahead bias; `test_no_lookahead_a_surviving_weeks_low_cannot_trigger_next_weeks_ratcheted_stop` in the test file is the regression guard for it.
  - Still-open positions mark to market at the last available **daily** close now (not weekly). `holding_days` replaced `holding_weeks` since exits can land mid-week. The returned dict still carries both `stop_price` (the level that triggered) and `exit_price` (what was filled) — now equal by construction for a stopped-out trade, since fills happen at the stop price itself.
- `load_basic_trail_tickers(folder)` — `INPUT/`, else the **newest file in `OUTPUT/BasicScan/`** (the Basic Scanner's own last output) — deliberately *not* the Darvas Raw screener fallback `backtest_signal.py` uses, since silently testing against a completely different, uptrend-biased universe would defeat the point of "based on this scanner."
- No dollar notional anywhere in this script — pure `return_pct`, by explicit user request (a platform for comparing settings, not simulating account size).
- Summary stats (console/log + no per-row repetition in the CSV): total trades, closed/open, stopped-out-count vs won-count (split by `return_pct` sign, not by exit mechanism — every closed trade technically exits via the trailing stop, so "stopped out" here means "closed at a loss"), and separately for wins and for losses: min/max/avg % vs entry.
- Ships with `RUN Stoch Basic Trail Backtest.command`. 7 tests in `tests/test_backtest_basic_trail.py` (hand-computed fixtures, same style as `test_backtest.py`), full suite 77/77 passing.
- **Not built yet, deliberately**: any entry filter beyond the raw crossover (e.g. an EMA trend gate) — explicitly deferred as a future addition once this first edition's results are seen, same "start loose, tighten later" philosophy as the Basic Scanner itself.

## Folder Structure

Everything generated by a run lives under `OUTPUT/` (gitignored wholesale, see `.gitignore`) — nothing writes to the project root any more (as of 2026-08-26; before that, `screener.py`/`stoch_scan.py` wrote dated CSV/txt/log files directly into the root, which is what `OUTPUT/Screener/` and `OUTPUT/SweetSpot/` replaced).

- `INPUT/` — drop a TradingView Watchlist export here for `backtest.py` to pick up (see the two accepted filename patterns above), or any file for `backtest_signal.py`/`backtest_signal_sltp_sweep.py` (newest file wins, no naming requirement). Falls back to `OUTPUT/Screener/`'s latest screener CSV if empty.
- `OUTPUT/Screener/` — `screener.py`'s `Stoch Raw Screener_YYYY-MM-DD.csv` + its `stoch_screener_log_YYYY-MM-DD.txt`. Read by `stoch_scan.py`, `stoch_fresh_scan.py`, and `backtest.py` (as its `INPUT/`-fallback) via `find_latest_csv()`.
- `OUTPUT/SweetSpot/` — `stoch_scan.py`'s `Stoch_SweetSpot_YYYY-MM-DD.txt` + its `stoch_scan_log_YYYY-MM-DD.txt`.
- `OUTPUT/BasicScreener/` — `stoch_basic_screener.py`'s `Stoch Basic Screener_YYYY-MM-DD.csv` + its log. Independent from `OUTPUT/Screener/` — a different universe (looser filters), not a replacement.
- `OUTPUT/BasicScan/` — `stoch_basic_scan.py`'s `Stoch_BasicScan_YYYY-MM-DD.txt` (plain ticker list, for direct TradingView watchlist import) + its log.
- `OUTPUT/` (flat) — every other script's output: `backtest.py`, `backtest_signal.py`, `backtest_signal_sweep.py`, `backtest_signal_sltp_sweep.py`, `stoch_signal_scan.py`, `stoch_fresh_scan.py` — trades CSVs, candidate CSVs, sweep CSVs/MD, and their logs.
- All created automatically (`os.makedirs(..., exist_ok=True)`) by the script that needs them.

## Docs

- `docs/superpowers/specs/2026-07-14-stoch-backtester-design.md` — backtester design spec (entry/exit rules, folder structure, out-of-scope items)
- `docs/superpowers/plans/2026-07-14-stoch-backtester.md` — implementation plan (historical record of the 7-task build)

## Domain Vocabulary

- **Sweet spot band** — %K between 32 and 80 on the *weekly* Stochastic. The strategy's core signal zone.
- **%K / %D** — Stochastic fast line (red) / slow signal line (yellow) in the strategy doc's terminology.
- **Weekly Stoch** — the weekly Stochastic, computed off daily OHLC resampled to `W-FRI` bins. Settings are `[stoch_scanner]` in `stoch_config.ini` — currently 19/4/4, "Weekly Stochastic Sweet Spot Strategy.md"'s original settings (briefly swapped to the sweep-optimal 3/7/7 on 2026-07-23, reverted back 2026-07-30 — see Open Threads below). Never the daily 10/3/3 Stochastic mentioned in that doc (shown only for contrast there, not used for decisions).

## Open Threads (as of 2026-07-14)

Backtest of the confirmed mechanical rules across a 30-ticker watchlist (1,060 trades, full history): **43.9% win rate**, avg win +19.6%, avg loss -6.6%, avg hold ~8.8 weeks — notably below the strategy doc's claimed ~70% win rate, but with a strong enough win/loss skew that expectancy is still positive.

User observation from chart review: the mechanical single-shot exit appears to cut many trades short during otherwise-intact uptrends (visible on WT's chart — multiple exit signals fire within one larger trend). Under discussion, not yet built: a trailing-stop variant — move stop to breakeven on the first exit signal, then ratchet to each subsequent confirmed swing low, only truly exiting on a stop hit rather than the raw indicator signal. This would need to be spec'd (swing-low detection logic) and backtested against the same sample before deciding whether to adopt it. Also still pending: the "approaching 32" tightened live-scanner filter (deferred from the backtester work, kept as a fully separate follow-up per user request).

## Open Threads (as of 2026-07-21)

**Stochastic-parameter sweep completed** (`backtest_signal_sweep.py`, separate rule set from the strategy doc's original 19/4/4 — see `backtest_signal.py`/`backtest_signal_sweep.py` sections). Optimal combo by total $ P&L across the Darvas Raw screener universe (344 tickers, `auto_adjust=False`): **`stoch_length=3, k_smooth=7, d_smooth=7`** (thresholds fixed at 32/80) → $826,560.63 total P&L, 75.93% win rate, 2,061 closed trades. Applied live to the "Stochastic Weekly Signal" indicator on the AAPL/weekly chart (`chart 7m43lgZv`) for visual observation. **Adopted 2026-07-23** — see below. Not yet done: swapping the entry/exit thresholds (32/80) into the sweep (explicitly deferred, see `backtest_signal_sweep.py` above), and re-running the sweep once/if a different ticker universe or objective (e.g. risk-adjusted return instead of raw total $) is wanted.

## Open Threads (as of 2026-07-23)

**`stoch_config.ini` `[stoch_scanner]` updated to the sweep-optimal `3/7/7`** (previously 19/4/4, under observation per the 2026-07-21 entry above). Since `stoch_scan.py`, `backtest_signal.py`, and `stoch_signal_scan.py` all read these constants from the shared config rather than hardcoding them, all three now run on 3/7/7 with no other code changes needed.

**Built `stoch_signal_scan.py`** — live buy-candidate scan (see Architecture above): flags Darvas Raw screener tickers whose weekly %K crossed strictly above 32 (with %K>%D) on the *latest* completed weekly bar, i.e. "fresh this week" only — distinct from `backtest_signal.py`'s full-history backtest and from `stoch_scan.py`'s in-band "sweet spot" scan. Buy-only by design (no notion of held positions, so sell signals aren't actionable from this scanner alone). Ships with `RUN Stoch Signal Scan.command`. 6 new tests in `tests/test_stoch_signal_scan.py`, full suite 38/38 passing. First live run (2026-07-23, post 3/7/7 adoption) found 1 fresh candidate: FANG (K:42.33, D:27.73, price $203.02) — see `OUTPUT/StochSignal_Candidates_2026-07-23.csv`.

## Open Threads (as of 2026-07-25)

**Built `ema50_screener.py` / `ema50_stoch_signal_scan.py`** — a second, independent "fresh buy candidate" pipeline, supplemental to `stoch_signal_scan.py` (see Architecture above). Full NASDAQ+NYSE universe, filtered to price≥$10 / avg vol(10d)>1M / ADR%>2 / close 0–5% above EMA50, then screened with the same `is_fresh_stoch_signal_buy()` check reused unchanged from `stoch_signal_scan.py`. Ships with `RUN EMA50 Stoch Scan.command`. No existing file was modified — `stoch_config.ini` only gained a new `[ema50_screener]` section. 12 new tests (`test_ema50_screener.py` + `test_ema50_stoch_signal_scan.py`).

**Retired 2026-07-26** — see that date's entry below. This whole standalone pipeline (files, tests, config section) was removed the next day once analysis showed its independent full-market fetch wasn't adding anything the Darvas Raw screener didn't already provide; only its EMA-proximity idea survived, folded into `stoch_signal_scan.py`.

**Bug found and fixed on first live run of `ema50_screener.py`**: 0 candidates out of 6,628 tickers. Root cause: `yfinance` with `auto_adjust=True` leaves the most-recent trading day's OHLC as `NaN` until its dividend-adjustment pipeline catches up (volume is populated, prices aren't) — `_passes()` read `.iloc[-1]` directly, so the price check silently no-opped (`NaN < threshold` is `False`) and the EMA-band check always failed (`NaN`-based distance). Fixed with `df = df.dropna(subset=["close"])` as the first line of `_passes()`; second run found 305 real candidates, 11 with a fresh Stoch buy signal. This is a **latent, pre-existing defect in `screener.py`/`darvas_screener.py` too** (same unguarded `.iloc[-1]` pattern) — not fixed there, out of scope for this session, but worth remembering if either ever produces suspiciously lopsided rejection-reason counts.

**`backtest_signal.py` gained `INPUT/` support** (`load_backtest_signal_tickers()`) so it can be pointed at a scan's output directly: if `INPUT/` has any non-hidden file, uses the newest one (no filename/format requirement — e.g. drop in `ema50_stoch_signal_scan.py`'s candidate `.txt` as-is), else falls back to the Darvas Raw screener CSV as before. Verified live: copied `EMA50_StochSignal_2026-07-25.txt` (11 tickers) into `INPUT/`, ran `backtest_signal.py`, correctly loaded exactly those 11 and logged `Tickers loaded : 11 (INPUT/EMA50_StochSignal_2026-07-25.txt)`. `backtest.py`'s own `INPUT/` resolver (`scan_utils.find_ticker_list()`, filename-glob-restricted) was deliberately left untouched per an earlier explicit user decision (2026-07-23: "just leave it as it is").

## Open Threads (as of 2026-07-26)

**Built `backtest_signal_sltp_sweep.py`** (see Architecture above) — adds an SL/TP overlay (TP as an R-multiple of SL risk, both checked against daily intraweek highs/lows, first-hit-wins over the original signal exit) plus an optional entry filter on top of `backtest_signal.py`'s exact crossover rule. Ships with `RUN Stoch Signal SLTP Sweep.command`. Design decisions locked via user Q&A before building (TP as R-multiple not flat %; daily intraweek SL/TP checks not weekly-close-only; SL wins on same-day double-touch; overlay not replace vs. the signal exit).

**Same-day iteration, in order**: (1) First run (SL 4-10% × TP 1.5-2.5R × EMA10>EMA20 filter on/off = 42 combos) against the 11-ticker EMA50 candidate list in `INPUT/`: best combo SL=10%/TP=2.5R/no filter → $3,252.84, 36.3% win rate; the short EMA10>20 filter was inconclusive (too few surviving trades to trust). (2) Per-ticker analysis of NOG/EQT/NVDA (the worst performers) found SL dominates exits (~68% in the best combo) and that NOG/EQT sit below the TP-multiple's breakeven win rate (`1/(1+TP_r)`) while NVDA sits well above it — a structural, not tuning, problem for NOG/EQT at high TP. (3) Expanded `TP_GRID` to include 3.0/3.5R per user request; new best became SL=9%/TP=3.5R/no filter → $4,611.47, 32.1% win rate. (4) Replaced the short EMA10>EMA20 filter with a long-term `EMA50>EMA200` (weekly) regime filter, since NOG/EQT's losers cluster inside multi-month/year downtrends, not short-term noise — `FILTER_GRID` is now `["none","long_trend"]`, `use_ema_filter: bool` became `filter_type: str`. (5) Verified: at SL=9%/TP=3.5R the long_trend filter cuts NOG's losses 17→5 (flips to slightly positive) and EQT's 30→19, but also cuts NVDA's best trades (win rate 50%→33.3%, P&L $2,506.53→$481.53) — aggregate portfolio total ends up lower filtered than unfiltered, because NVDA was carrying most of the unfiltered result. **Conclusion so far: the long-trend filter helps the weak/cyclical names specifically but isn't a universal improvement across a mixed universe** — worth exploring per-ticker-conditional filtering, or testing against a larger/different universe, before adopting it wholesale. 9 tests in `tests/test_backtest_signal_sltp_sweep.py`, full suite 64/64 passing throughout.

**Retired `ema50_screener.py` / `ema50_stoch_signal_scan.py`, folded the surviving idea into `stoch_signal_scan.py`.** User's insight: the Darvas Raw screener (and Stoch's own `[screener]`, which mirrors it) already requires EMA50>EMA200 trend confirmation and ATH-proximity ≤5% — exactly the kind of long-term trend quality gate that would have kept NOG/EQT's secular-downtrend periods out in the first place. `ema50_screener.py`'s independent full-market fetch never checked trend or ATH-proximity at all, which is *why* it let those weaker names through; its only genuinely new idea was the "not stretched" (price close to, not far above, EMA50) check. Deleted `ema50_screener.py`, `ema50_stoch_signal_scan.py`, `RUN EMA50 Stoch Scan.command`, their tests, and `[ema50_screener]` from `stoch_config.ini` (all untracked, added earlier the same day — nothing lost). Added `is_not_stretched()` to `stoch_signal_scan.py` (new `[stoch_signal_scan]` config section: `ema_length`/`ema_band_min_pct`/`ema_band_max_pct`, same 50/0/5 defaults) — a candidate now only makes the final list if it has both a fresh crossover **and** isn't stretched more than 5% above its daily EMA50. Verified live: XOM had a fresh signal but was correctly skipped as stretched (7.47% above EMA50); VNOM passed both checks (1.97% above). Full suite: 56/56 passing.

## Open Threads (as of 2026-07-30)

**Built `stoch_fresh_scan.py`** (see Architecture above) — a "rev" of the Sweet Spot scan: instead of flagging tickers currently sitting inside the persistent 32–80 band, flags tickers whose weekly %K crossed strictly above 32 (with %K>%D) within the last 2 **fully completed** weeks — a fresh-entry signal with a wider lookback than `stoch_signal_scan.py`'s single-week check, and no EMA50 "not stretched" gate. Design decisions locked via user Q&A: buy-side only (an earlier "sell signal" framing was a false start, discarded); current, still-forming week always excluded from the freshness check (not just the sibling scan's 1-week lookback that treats the live bar as eligible); reuses the exact same engine as `stoch_scan.py` (`compute_kd`, `[stoch_scanner]` settings, Stoch's own screener output) rather than the Darvas universe `stoch_signal_scan.py` depends on, chained in one `.command` operation (`screener.py && stoch_fresh_scan.py`); new independent `[stoch_fresh_scan]` config section (`lookback_weeks`, `entry_level`) rather than overloading `[stoch_scanner]`; EMA50 proximity filter explicitly declined for this scan (the user initially agreed to keep it, then reversed that decision). Ships with `RUN Stoch Fresh Scan.command`. 9 new tests in `tests/test_stoch_fresh_scan.py`, full suite 65/65 passing. First live run (2026-07-30, 307-ticker Stoch screener universe): 0 fresh candidates — plausible market-dependent result, not an error (log/CSV output verified correct shape).

**`[stoch_scanner]` config-vs-doc discrepancy resolved: 19/4/4 is intentional.** Flagged earlier the same session (comment block still said "3/7/7 adopted 2026-07-23" while the literal values on disk were 19/4/4). User confirmed: this is a deliberate reversion back to the strategy doc's original 19/4/4, not a bug — 19/4/4 is the current, correct setting. `stoch_config.ini`'s `[stoch_scanner]` comment and `CLAUDE.md`'s Configuration/Domain Vocabulary sections above updated to reflect this. Every script reading `[stoch_scanner]` (`stoch_scan.py`, `backtest_signal.py`, `stoch_signal_scan.py`, `stoch_fresh_scan.py`) now correctly runs on 19/4/4.

**`stoch_fresh_scan.py` dividend-adjustment bug found and fixed, same day.** User ran the live scan for real (separately from the dry run above) and got 1 candidate (WEX), but flagged that EQNR — expected to qualify — didn't show up. Root cause (see the `stoch_fresh_scan.py` Architecture section above for full detail): `auto_adjust=True` smeared EQNR's real one-week crossover across two of our weekly bars, missing it entirely. User also independently widened `[screener]`'s `ath_max_dist_pct` from 5.0 to 10.0 in this window — confirmed unrelated to the bug (EQNR already passed at 5%, so it passes at 10% too; no need to revert to isolate the fix). Switched `stoch_fresh_scan.py`'s fetch to `auto_adjust=False`. Verified: full suite still 65/65 passing (no test touches the fetch path), and a fresh `screener.py && stoch_fresh_scan.py` run (488-ticker universe under the new 10% ATH threshold) correctly classified EQNR as `weeks_ago=1` (K=44.08, D=34.5, matching the user's TradingView export to within 0.01) among 7 total candidates (CHRD, SFD, BDX, CF, EPRT, EQNR, WEX).

## Open Threads (as of 2026-08-26)

**Root-level output relocated under `OUTPUT/`.** `screener.py` and `stoch_scan.py` were the last two scripts still writing dated CSV/txt/log files directly into the project root (`Stoch Raw Screener_*.csv`, `stoch_screener_log_*.txt`, `Stoch_SweetSpot_*.txt`, `stoch_scan_log_*.txt`) — every other script in the project already wrote to `OUTPUT/`. Moved them to new `OUTPUT/Screener/` and `OUTPUT/SweetSpot/` subfolders (existing dated files moved along with them, nothing deleted). Required updating every reader of the screener CSV, not just the two writers: `stoch_scan.py`'s and `stoch_fresh_scan.py`'s `find_latest_csv(folder)` calls, and `backtest.py`'s `find_ticker_list(input_dir, folder)` fallback, all now point at `OUTPUT/Screener/` instead of the project root. `.gitignore`'s now-redundant root-level filename patterns (superseded by the existing blanket `OUTPUT/*` rule) were removed. No script logic changed — this is I/O path plumbing only; full suite still 65/65 passing, and `find_latest_csv()` verified live against the relocated files.

**Also, separately: leftover `ema50_screener.py` output artifacts cleaned up.** `EMA50 Raw Screener_2026-07-25.csv` and its two log files (root) plus `INPUT/EMA50_StochSignal_2026-07-25.txt` — all produced by the pipeline retired on 2026-07-26 (see that date's entry above) — were still sitting on disk with no code left to produce or consume them. The `INPUT/` file in particular was a live hazard: `backtest_signal.py`/`backtest_signal_sltp_sweep.py` pick "the newest file in `INPUT/`, any name" as their ticker universe, so it would have silently narrowed any future run to that stale 11-ticker list instead of the full Darvas universe. All four deleted.

## Open Threads (as of 2026-08-27)

**Built the Stochastic Basic Scanner** (`stoch_basic_screener.py` + `stoch_basic_scan.py`, see Architecture above) after the user observed that every existing scanner's candidates were consistently stretched — a direct consequence of `[screener]`'s `ema_fast`/`ema_slow` uptrend and `ath_max_dist_pct` proximity-to-high filters, which the Basic Scanner deliberately drops. Designed via `superpowers:brainstorming` (bounded path): loose filters only (no trend/ATH-proximity gate, $300M market-cap floor), and a pure current-bar snapshot condition (%K>%D and %K<32, no "rising" requirement, unlike the Sweet Spot scan) — all explicit user decisions, meant to be tightened later once real output is seen, not now. Shared-code cleanup done alongside: `screener.py`'s `_fetch_universe()`/`_adr()` moved into `scan_utils.py` as public `fetch_universe()`/`adr()`. 5 new tests, ships with `RUN Stoch Basic Scan.command`. First live run (2026-08-27, 6,659-ticker universe): 1,056 passed the loose screener, **94 candidates** found, 0 errors.

**Basic Scan output format changed from CSV to plain-text mid-session.** Originally saved as a CSV (`ticker`, `k`, `d`, `price`); user asked for a plain ticker list instead (one per line, same format as `Stoch_SweetSpot_*.txt`) for direct TradingView watchlist import — no other technical data needed in the file. Changed `stoch_basic_scan.py`'s final write accordingly (K/D/price still print to console/log during the run, just aren't saved to the output file). The already-fetched 2026-08-27 result was converted to the new format rather than re-running the 8–10 min live fetch. User has since added a TradingView alert on the full 94-ticker list and is watching it develop before deciding on any filter changes.

**Built `backtest_basic_trail.py`** (see Architecture above) — a trailing-stop-only backtest of the Basic Scanner's premise, in the same session. Design negotiated over several rounds via `superpowers:brainstorming` (bounded path): (1) entry = `backtest_signal.py`'s exact crossover, confirmed rather than assumed; (2) ticker universe = a fixed snapshot (`INPUT/` or the newest `OUTPUT/BasicScan/` file), not a full historical walk-forward simulation of the scanner itself; (3) trail computed off highest weekly **close** (not intraday high) — kept in the daily-intraweek revision below; (4) trailing stop is the **sole** exit, no signal-exit fallback (unlike `backtest_signal_sltp_sweep.py`); (5) no dollar notional at all — user explicitly wants pure %-return statistics: total/closed/open counts, stopped-out-vs-won counts (split by `return_pct` sign, not exit mechanism), and separate min/max/avg % for wins and for losses. Deliberately not built: any entry filter beyond the raw crossover (e.g. EMA trend) — user's own framing was "first edition," filters to be added later once results are seen.

**Live run against the 94-ticker Basic Scan watchlist, then revised to daily intraweek stop-checking, same day.** First run (weekly-close-only check, full history, `auto_adjust=False`): 1,573 trades, 957 stopped out / 607 won (38.8% win rate), avg hold 34.4 weeks. Mean win (72.77%) was wildly skewed by one outlier — QCOM entered 1998-10-30 at $3.48 during the dot-com run-up, exited 2000-01-14 at $70.22, +1,920% over 63 weeks (real, not a bug). Median win (32.77%) and median loss (-17.69%) are the representative numbers. The worst loss, however, exposed a real design gap: VERA entered 2022-12-23 at $19.90 (stop nominally $15.92, still at its initial level) and exited just 2 weeks later at $5.48 — **-72.46%** against a nominal -20% stop, because the original check only looked at each week's *close*, so a single-week crash (almost certainly a biotech trial-failure headline) blew straight through the stop with no intraweek protection. User explicitly wants this to model live trade supervision realistically, so `simulate_trades_basic_trail()` was revised to check daily lows within each week instead (see Architecture above for the full mechanics and the look-ahead-avoidance sequencing this required). 7 tests rewritten with daily+weekly fixtures (one is a dedicated look-ahead regression guard), full suite still 77/77 passing. **Not yet re-run live** against the 94-ticker watchlist under the new daily-intraweek logic — do that before drawing any conclusions from the stats above, they're from the superseded weekly-close-only version.
